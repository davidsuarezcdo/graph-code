{
  "name": "graph-code",
  "module": "src/index.ts",
  "type": "module",
  "scripts": {
    "compile": "tsc",
    "build:graph": "bun run src/index.ts",
    "serve:chat": "python -m streamlit run ai/run_chat_server.py",
    "serve:mcp": "python ai/run_mcp_server.py 28888"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "neo4j-driver": "^5.28.1"
  }
}
